[phases.setup]
aptPkgs = [
    "libnss3",
    "libxss1",
    "libasound2",
    "libatk-bridge2.0-0",
    "libgtk-3-0",
    "libgbm-dev",
    "libnss3"
]

[phases.build]
cmds = [
    "pip install -r requirements.txt",
    "python -m playwright install chromium"
]
